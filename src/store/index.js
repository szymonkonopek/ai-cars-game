import { createStore } from "vuex";
import auth from "@/store/modules/auth";
import gptCars from "@/store/modules/gptCars";

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: { auth, gptCars },
});
